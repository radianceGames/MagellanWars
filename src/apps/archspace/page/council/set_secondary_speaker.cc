#include <libintl.h>
#include "../../pages.h"
#include "../../archspace.h"
#include "../../game.h"

bool
CPageSetSecondarySpeaker::handler(CPlayer *aPlayer)
{
	/*ARE_YOU_LEAD_SPEAKER(aPlayer);
	CCouncil *Council = aPlayer->get_council();
	QUERY("COUNCIL_SECONDARY_SPEAKER", sSecondarySpeaker);
	if(!sSecondarySpeaker)
	{
		return false;
	}
	int iSecondarySpeaker = as_atoi(sSecondarySpeaker);

	Council->set_secondary_speaker(iSecondarySpeaker);

	ITEM("RESULT_STRING",format("%s is now the new Secondary Speaker.",aPlayer->get_name());
	return output("council/speaker_menu_change_council_secondary_speaker_result.html");*/
}